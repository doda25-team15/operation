apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.labels.app.name }}-deployment-canary-v2
  labels:
    app: {{ .Values.labels.app.app }}
    component: {{ .Values.labels.app.component }}
    version: v2
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
spec:
  replicas: {{ .Values.replicaCount.app }}
  selector:
    matchLabels:
      app: {{ .Values.labels.app.app }}
      component: {{ .Values.labels.app.component }}
      version: v2
  template:
    metadata:
      labels:
        app: {{ .Values.labels.app.app }}
        component: {{ .Values.labels.app.component }}
        version: v2
    spec:
      containers:
      - name: app
        image: "{{ .Values.image.app.repository }}:{{ .Values.image.app.canarytag }}"
        imagePullPolicy: {{ .Values.image.app.pullPolicy }}
        ports:
        - containerPort: {{ .Values.service.app.port }}
        envFrom:
          - configMapRef:
              name: {{ .Values.configMap.name }}
