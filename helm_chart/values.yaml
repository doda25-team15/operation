replicaCount:
    app: 2
    model: 2

labels:
    app:
        name: app
        component: app
        app: sms-checker
        team: doda25-team15
    model:
        name: model-service
        app: model-service

image:
    app:
        repository: ghcr.io/doda25-team15/app
<<<<<<< HEAD
        tag: v1.0.6
=======
        tag: v1.0.8
>>>>>>> 751c91e50f0d79140a86b2793fd76c7899c5a04a
        pullPolicy: IfNotPresent
    model:
        repository: ghcr.io/doda25-team15/model-service
        tag: v1.0.2
        pullPolicy: IfNotPresent

service:
    type: ClusterIP
    app:
        name: app-service
        port: 8080
        targetPort: 8080
    model:
        name: model-service
        port: 8081
        targetPort: 8081

ingress:
    enabled: true
    name: ingress
    className: nginx
    host: sms-checker-app

volume:
    name: volume-cache
    hostPath: /mnt/shared/output

volumeMounts:
    name: volume-cache
    mountPath: /app/output

configMap:
  name: env-config-map

secret:
  name: secret
  smtpUser: "user"
  smtpPass: "password"

monitoring:
  prometheus:
    prometheusSpec:
      serviceMonitorSelector:
        matchLabels:
          team: doda25-team15
    service:
      sessionAffinity: ""

  alertmanager:
    service:
      sessionAffinity: ""

  coreDns:
    enabled: false
  kubeEtcd:
    enabled: false
  kubeControllerManager:
    enabled: false
  kubeScheduler:
    enabled: false
  kubeProxy:
    enabled: false

  grafana:
    enabled: true
    sidecar:
      dashboards:
        enabled: true
        label: grafana_dashboard
        labelValue: "1"
        searchNamespace: ALL
istio: 
  gateway: 
    name: sms-checker-gateway