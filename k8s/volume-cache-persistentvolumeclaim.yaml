apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: volume-cache
  name: volume-cache
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
