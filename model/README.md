# Model

This folder contains an example model deployment that can be used to mount to the model-service in the Kubernetes cluster.

See [README.md](../README.md#setup-model-files-volume-mount) in the main `operation` folder for instructions on how to use these model files with minikube.
